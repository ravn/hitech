# Vendored Z80-target runtime — see README.md for provenance.
# Override the repo-root rules for files in this subtree.

# Libraries and startup objects must be byte-identical to upstream.
lib80/*.LIB binary
lib80/*.OBJ binary

# Headers are plain ASCII; let git normalize line endings on commit.
include80/*.H text
